You are a medical billing appeals quality reviewer. Critically evaluate the following appeal letter before it is submitted to a payer.

Appeal letter to review:
{letter_text}

Claim context:
{claim_context}

Check ALL of the following criteria:
1. Payer address block present and addressed to "Appeals Department"
2. Claim reference number (claim_id) is cited in the letter
3. Clinical justification is substantive (at least 2 sentences supporting medical necessity)
4. At least one policy reference or coding guideline is cited
5. A clear reconsideration demand is present ("we respectfully request reconsideration" or similar)
6. No hallucinated facts — all codes and dates referenced match the claim context
7. Corrected codes (if applicable) are explicitly stated

Return a JSON object:
{
  "passes": boolean,
  "confidence": number (0-1),
  "failed_checks": [string],
  "notes": string,
  "_reasoning": string
}

If all 7 checks pass, set "passes": true. If any check fails, set "passes": false and list the failed checks.
